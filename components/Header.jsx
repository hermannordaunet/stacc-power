import styles from "./Header.module.css";

export function Header() {
  return (
    <header className={styles.header}>
      <h1>This is not were my work is done. I focused on API endpoints</h1>
    </header>
  );
}
